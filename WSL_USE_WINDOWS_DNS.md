# WSL 共用 Windows DNS 配置指南

本文档用于让 WSL 通过 `dnsTunneling` 共用 Windows 的 DNS 解析能力。

这种方式尤其适合以下场景：

- Windows 连接了 Cisco 等企业 VPN；
- WSL 需要解析企业内部域名；
- DNS 地址会随着 Wi-Fi、VPN或网络环境变化；
- 不希望在 WSL 中长期写死公共 DNS 地址。

> `dnsTunneling` 可以让 WSL 通过 Windows 进行域名解析，但不能在 `.wslconfig` 中直接指定 `8.8.8.8`、`223.5.5.5` 等 DNS 服务器地址。

## 一、配置文件职责

| 配置文件 | 位置 | 作用 |
| --- | --- | --- |
| `.wslconfig` | Windows 用户目录 | 配置整个 WSL 2 虚拟机、网络模式和 DNS 隧道 |
| `wsl.conf` | WSL 的 `/etc/wsl.conf` | 配置某一个 Linux 发行版的启动和网络行为 |
| `resolv.conf` | WSL 的 `/etc/resolv.conf` | 保存 Linux 实际使用的 DNS 入口 |

本机对应的 Windows 配置路径是：

```text
C:\Users\admin\.wslconfig
```

项目中保存的配置模板是：

```text
D:\nets\.wslconfig
```

---

## 二、环境要求

建议先在管理员 PowerShell 中检查：

```powershell
wsl --version
wsl --status
```

如果 WSL 版本较旧，可以更新：

```powershell
wsl --update
```

DNS 隧道主要面向较新的 Windows 11 和 WSL 版本。如果系统或 WSL 版本不支持，配置项可能无法达到预期效果。

当前使用的 `bridged` 网络模式已经被微软标记为弃用。DNS 隧道与桥接模式组合后的行为可能因 WSL 版本不同而变化。

---

## 三、配置 `.wslconfig`

`D:\nets\.wslconfig` 应为：

```ini
[wsl2]
networkingMode=bridged
vmSwitch=wsl-net
dhcp=true
dnsTunneling=true
```

其中：

- `networkingMode=bridged`：让 WSL 使用外部桥接网络；
- `vmSwitch=wsl-net`：使用已经绑定到物理 Wi-Fi 的 Hyper-V 外部交换机；
- `dhcp=true`：允许 WSL 通过 DHCP 获取局域网地址；
- `dnsTunneling=true`：让 WSL 通过 Windows DNS 栈解析域名。

将模板复制到 Windows 用户目录：

```powershell
Copy-Item "D:\nets\.wslconfig" "C:\Users\admin\.wslconfig" -Force
```

确认内容：

```powershell
Get-Content "C:\Users\admin\.wslconfig"
```

预期输出：

```text
[wsl2]
networkingMode=bridged
vmSwitch=wsl-net
dhcp=true
dnsTunneling=true
```

---

## 四、处理 `/etc/wsl.conf`

### 情况 A：`/etc/wsl.conf` 不存在

这是正常情况，不需要仅仅为了 DNS 隧道而创建该文件。

文件不存在时，WSL 默认相当于使用：

```ini
[network]
generateResolvConf=true
```

这正是 DNS 隧道需要的行为，可以直接进行下一步。

确认文件是否存在：

```bash
if [ -f /etc/wsl.conf ]; then
    cat /etc/wsl.conf
else
    echo '/etc/wsl.conf 不存在，将使用 WSL 默认配置'
fi
```

### 情况 B：文件存在且配置了 `generateResolvConf=false`

编辑文件：

```bash
sudo nano /etc/wsl.conf
```

将：

```ini
[network]
generateResolvConf=false
```

修改为：

```ini
[network]
generateResolvConf=true
```

也可以删除整个 `[network]` 段，让 WSL 使用默认值。

### 情况 C：还需要启动时自动请求 DHCP

如果 WSL 每次启动后都没有 IPv4，必须手动执行 `dhclient -v eth0`，可以创建或修改 `/etc/wsl.conf`：

```bash
sudo nano /etc/wsl.conf
```

写入：

```ini
[boot]
command=/usr/sbin/dhclient -4 eth0

[network]
generateResolvConf=true
```

如果已经启用 systemd，应保留在同一个 `[boot]` 段中：

```ini
[boot]
systemd=true
command=/usr/sbin/dhclient -4 eth0

[network]
generateResolvConf=true
```

不要创建多个重复的 `[boot]` 或 `[network]` 段。

> `dhclient` 可能根据 DHCP 响应修改 `/etc/resolv.conf`。如果自动 DHCP 后 DNS 隧道地址被替换，应按照本文的故障排查部分检查，并避免同时使用“固定 `/etc/resolv.conf`”与 DNS 隧道方案。

---

## 五、重新生成 DNS 配置

如果之前手工创建过 `/etc/resolv.conf`，建议删除旧文件，让 WSL 在下一次启动时重新生成：

```bash
sudo rm -f /etc/resolv.conf
exit
```

然后在 Windows PowerShell 中完全关闭 WSL：

```powershell
wsl --shutdown
```

等待几秒后重新启动：

```powershell
wsl
```

> 只关闭当前终端窗口不一定会停止 WSL 2 虚拟机，必须执行 `wsl --shutdown` 才能确保 `.wslconfig` 被重新加载。

---

## 六、验证 DNS 隧道

进入 WSL 后检查：

```bash
cat /etc/resolv.conf
```

正常情况下，该文件应由 WSL 自动生成，并包含一个 WSL 管理的 DNS 地址，例如：

```text
# This file was automatically generated by WSL.
nameserver <WSL 管理的 DNS 地址>
```

该地址不一定是局域网网关 `10.10.0.1`，也不一定等于 Windows 网卡中显示的实际 DNS 地址。这是正常的：Linux 将 DNS 请求交给 WSL DNS 隧道，再由 Windows DNS 栈处理。

依次验证：

```bash
ip -4 addr show dev eth0
ip route
ping -c 4 10.10.0.1
ping -c 4 1.1.1.1
getent hosts www.baidu.com
ping -c 4 www.baidu.com
```

正常桥接路由应类似：

```text
default via 10.10.0.1 dev eth0
10.10.0.0/16 dev eth0 proto kernel scope link src 10.10.250.212
```

结果判断：

| 结果 | 说明 |
| --- | --- |
| 网关、`1.1.1.1` 和域名都正常 | 桥接和 DNS 隧道均正常 |
| `1.1.1.1` 正常，域名失败 | DNS 隧道或 `/etc/resolv.conf` 有问题 |
| 网关正常，`1.1.1.1` 失败 | 默认路由、VPN 或上游网络策略问题 |
| 网关失败 | DHCP、桥接交换机或 Wi-Fi 链路问题 |
| `eth0` 没有 IPv4 | DHCP 尚未成功，与 DNS 无关 |

---

## 七、验证 Windows 当前 DNS

在 Windows PowerShell 中执行：

```powershell
Get-DnsClientServerAddress -AddressFamily IPv4 |
    Where-Object { $_.ServerAddresses.Count -gt 0 } |
    Format-Table InterfaceAlias, ServerAddresses
```

重点检查：

- `vEthernet (wsl-net)`；
- Cisco VPN 对应的网络接口；
- 当前具有默认路由的接口。

验证 Windows 本身能否解析域名：

```powershell
Resolve-DnsName www.baidu.com
```

如果 Windows 本身也无法解析，WSL DNS 隧道不会解决 Windows 侧的 DNS 故障。应先修复 Windows、Wi-Fi 或 VPN 的 DNS 配置。

查看 Windows 默认路由：

```powershell
Get-NetRoute -AddressFamily IPv4 -DestinationPrefix "0.0.0.0/0" |
    Sort-Object RouteMetric |
    Format-Table InterfaceAlias, NextHop, RouteMetric, InterfaceMetric
```

本机同时存在 Wi-Fi 桥接网关和 Cisco VPN 网关。连接 VPN 后，Windows DNS 和路由可能根据 VPN 策略变化，这正是使用 DNS 隧道而不是固定公共 DNS 的主要原因之一。

---

## 八、常见问题

### 1. `/etc/wsl.conf` 不存在

正常，不需要仅为 DNS 隧道创建。文件不存在时，`generateResolvConf=true` 是默认行为。

### 2. `/etc/resolv.conf` 不存在

确保没有设置：

```ini
[network]
generateResolvConf=false
```

然后执行：

```powershell
wsl --shutdown
wsl
```

WSL 应重新生成该文件。

### 3. `/etc/resolv.conf` 仍是以前手工配置的 DNS

在 WSL 中删除旧文件：

```bash
sudo rm -f /etc/resolv.conf
exit
```

然后在 Windows 中执行：

```powershell
wsl --shutdown
wsl
```

重新检查：

```bash
cat /etc/resolv.conf
```

### 4. `ping www.baidu.com` 提示 `Temporary failure in name resolution`

先验证公网 IP：

```bash
ping -c 4 1.1.1.1
```

如果公网 IP 正常，执行：

```bash
cat /etc/resolv.conf
getent hosts www.baidu.com
```

同时确认 Windows 可以解析：

```powershell
Resolve-DnsName www.baidu.com
```

### 5. 手动运行 `dhclient` 后 DNS 又失效

检查运行前后的 DNS 文件：

```bash
cat /etc/resolv.conf
sudo dhclient -v eth0
cat /etc/resolv.conf
```

如果 `dhclient` 将 WSL DNS 隧道地址替换成了不可用的 DHCP DNS，说明 DHCP 客户端与 WSL 自动 DNS 发生冲突。

可以先执行一次完整重启，让 WSL 恢复 DNS 隧道配置：

```powershell
wsl --shutdown
wsl
```

如果每次都复现，需要在以下两种方案中选择一种：

1. 保留 `dnsTunneling=true`，调整 DHCP 启动方式，避免覆盖 WSL 生成的 DNS；
2. 放弃 DNS 隧道，设置 `generateResolvConf=false`，在 WSL 中明确配置 DHCP 或 Windows 实际使用的 DNS。

排查时应保存以下输出：

```bash
ip -4 addr show dev eth0
ip route
cat /etc/resolv.conf
```

### 6. 公共域名能解析，但企业内部域名不能解析

确认 Windows 在连接 VPN 后可以解析该内部域名：

```powershell
Resolve-DnsName 内部域名
```

如果 Windows 可以而 WSL 不可以，检查：

- `dnsTunneling=true` 是否已经复制到 `C:\Users\admin\.wslconfig`；
- 是否执行过 `wsl --shutdown`；
- `/etc/wsl.conf` 是否禁用了 `generateResolvConf`；
- `/etc/resolv.conf` 是否被 `dhclient` 或其他网络管理器覆盖；
- WSL 和 Windows 版本是否支持当前组合。

---

## 九、不要同时使用的配置

使用 Windows DNS 隧道时，不要同时执行下面这些操作：

1. 不要设置：

   ```ini
   [network]
   generateResolvConf=false
   ```

2. 不要长期手工写死 `/etc/resolv.conf`。
3. 不要对 `/etc/resolv.conf` 执行 `chattr +i`。
4. 不要假设 WSL DNS 地址必须等于 `10.10.0.1`。
5. 不要仅通过 `ping www.baidu.com` 判断整个网络是否中断；应先测试网关和公网 IP。

如果以前执行过不可变属性设置，可恢复：

```bash
sudo chattr -i /etc/resolv.conf 2>/dev/null || true
```

然后删除旧文件并重启 WSL：

```bash
sudo rm -f /etc/resolv.conf
exit
```

```powershell
wsl --shutdown
wsl
```

---

## 十、完整执行清单

### Windows 管理员 PowerShell

```powershell
Copy-Item "D:\nets\.wslconfig" "C:\Users\admin\.wslconfig" -Force
Get-Content "C:\Users\admin\.wslconfig"
wsl --shutdown
wsl
```

### WSL

```bash
cat /etc/resolv.conf
ip -4 addr show dev eth0
ip route
ping -c 4 10.10.0.1
ping -c 4 1.1.1.1
getent hosts www.baidu.com
ping -c 4 www.baidu.com
```

### Windows DNS 对照测试

```powershell
Resolve-DnsName www.baidu.com
Get-DnsClientServerAddress -AddressFamily IPv4 |
    Where-Object { $_.ServerAddresses.Count -gt 0 } |
    Format-Table InterfaceAlias, ServerAddresses
```

全部通过后，说明：

- WSL 已通过 Wi-Fi 外部交换机获得独立局域网地址；
- WSL 默认路由工作正常；
- WSL DNS 请求已交给 Windows DNS 栈处理；
- Windows Wi-Fi 或 VPN 的 DNS 变化可以由 WSL DNS 隧道跟随。
